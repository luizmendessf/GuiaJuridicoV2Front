# API (opcional em dev; produção usa .env.production ou variável no host)
# VITE_API_URL=http://localhost:8080/api

# Google Sign-In — OAuth 2.0 Web Client ID (mesmo valor do GOOGLE_CLIENT_ID no backend)
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
